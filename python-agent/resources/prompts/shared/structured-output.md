Return exactly one JSON object and nothing else. Do not use Markdown fences, prose, comments, or undefined fields. Field names, casing, enum values, required fields, and data types must exactly match this schema:

{{schema_json}}

Formatting-only example input:
{{few_shot_input}}

Formatting-only example output:
{{few_shot_output}}
