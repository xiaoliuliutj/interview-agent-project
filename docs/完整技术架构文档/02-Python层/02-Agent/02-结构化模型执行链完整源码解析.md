# Agent 公共执行链：简历评价、结构化输出、重试与提示加载

## 1. 接口定义

本章分析 Python Agent 公共模型执行链。直接 HTTP 入口是 `POST /v1/agent/evaluate/resume`，它调用 `ResumeEvaluationAgent.evaluate` 生成结构化简历评价；相同的 `StructuredOutputInvoker`、`AsyncRetryExecutor` 和 `PromptLoader` 还被面试初始化、回答、总结与网页抓取规划等接口复用。

这条链的职责是把业务 Prompt 和输入转换为模型消息，执行可配置的单次超时与有限重试，把多种模型响应形态还原成文本，清理 JSON 围栏，使用 Pydantic schema 校验，并在输出格式不合法时进行有限轮纠错。行号均对应当前工作区源码。

## 2. 函数调用链

```text
服务装配：
build_resume_evaluation_agent
  -> PromptLoader.__init__
  -> AsyncRetryExecutor.__init__
  -> ResumeEvaluationAgent.__init__
     -> StructuredOutputInvoker.__init__

简历评价：
POST /v1/agent/evaluate/resume
  -> evaluate_resume
  -> ResumeEvaluationAgent.evaluate
     -> SkillRegistry.get
     -> PromptLoader.render
        -> PromptLoader.load
           -> PromptLoader._resolve
        -> PromptLoader.render.<locals>.replace（模板每个占位符）
     -> StructuredOutputInvoker.invoke
        -> PromptLoader.render（shared/structured-output.md）
           -> PromptLoader.load
              -> PromptLoader._resolve
           -> PromptLoader.render.<locals>.replace
        -> _few_shot_output
        -> AsyncRetryExecutor.max_output_correction_attempts
        -> StructuredOutputInvoker._invoke_model
           -> RawChatModel.ainvoke（未配置重试器）
           或 AsyncRetryExecutor.execute（配置重试器）
              -> RawChatModel.ainvoke
              -> AsyncRetryExecutor._is_retryable（异常时）
              -> AsyncRetryExecutor._backoff_seconds（仍可重试时）
        -> StructuredOutputInvoker._validate
           -> _content_as_text
           -> _strip_json_fence
           -> schema.model_validate
        -> _readable_validation_error（校验失败时）
        -> _content_as_text（生成纠错上下文时）
        -> StructuredOutputInvoker._invoke_model（仍有纠错次数时再次执行）

其他面试、总结、Skill 选择和网页抓取规划 Agent：
业务 Agent 函数
  -> PromptLoader.render
  -> StructuredOutputInvoker.invoke
  -> 与上述格式约束、模型调用、重试、解析、校验和纠错链相同
```

## 3. 函数解析

### 3.1 `python-agent/app/agents/evaluation/agent.py` 完整源码

~~~python
"""基于简历事实、岗位要求与外置 Skill 的简历评价 Agent。"""

from app.tools.skills.loader import SkillRegistry
from app.common.prompt_loader import PromptLoader
from app.infrastructure.reliability.retry import AsyncRetryExecutor
from app.infrastructure.reliability.structured_output import RawChatModel, StructuredOutputInvoker

from .models import ResumeEvaluation


class ResumeEvaluationAgent:
    def __init__(
        self,
        model: RawChatModel,
        prompt_loader: PromptLoader,
        skill_registry: SkillRegistry,
        retry_executor: AsyncRetryExecutor | None = None,
    ) -> None:
        self._model = model
        self._prompt_loader = prompt_loader
        self._skill_registry = skill_registry
        self._retry_executor = retry_executor
        self._structured_output = StructuredOutputInvoker(prompt_loader, retry_executor)

    async def evaluate(
        self,
        *,
        subject_id: str,
        input_text: str,
        target_role: str,
    ) -> ResumeEvaluation:
        normalized_text = input_text.strip()
        if not normalized_text:
            raise ValueError("待评价内容不能为空")

        skill = self._skill_registry.get("resume-analyst")
        system_prompt = self._prompt_loader.render(
            "resume/analysis.md", {"skill_instructions": skill.instructions}
        )
        payload = {
            "subjectId": subject_id,
            "targetRole": target_role,
            "resumeText": normalized_text,
        }
        return await self._structured_output.invoke(
            model=self._model,
            schema=ResumeEvaluation,
            business_prompt=system_prompt,
            input_payload=payload,
        )
~~~

#### 3.1.1 `ResumeEvaluationAgent.__init__`

文件：`python-agent/app/agents/evaluation/agent.py:12-23`

逐行解释：

- 第 12 行：开始定义简历评价 Agent 构造函数。
- 第 13 行：接收当前实例。
- 第 14 行：接收满足 `RawChatModel` 协议的原始聊天模型。
- 第 15 行：接收 PromptLoader。
- 第 16 行：接收 SkillRegistry，用于取得外置的简历分析 Skill。
- 第 17 行：接收可选异步重试执行器。
- 第 18 行：声明构造函数不返回业务值。
- 第 19 行：保存模型。
- 第 20 行：保存 PromptLoader。
- 第 21 行：保存 SkillRegistry。
- 第 22 行：保存重试执行器。
- 第 23 行：调用项目构造函数 `StructuredOutputInvoker.__init__`，把 PromptLoader 和可选重试器绑定为统一结构化输出链。

#### 3.1.2 `ResumeEvaluationAgent.evaluate`

文件：`python-agent/app/agents/evaluation/agent.py:25-50`

```python
    async def evaluate(
        self,
        *,
        subject_id: str,
        input_text: str,
        target_role: str,
    ) -> ResumeEvaluation:
        normalized_text = input_text.strip()
        if not normalized_text:
            raise ValueError("待评价内容不能为空")

        skill = self._skill_registry.get("resume-analyst")
        system_prompt = self._prompt_loader.render(
            "resume/analysis.md", {"skill_instructions": skill.instructions}
        )
        payload = {
            "subjectId": subject_id,
            "targetRole": target_role,
            "resumeText": normalized_text,
        }
        return await self._structured_output.invoke(
            model=self._model,
            schema=ResumeEvaluation,
            business_prompt=system_prompt,
            input_payload=payload,
        )
```

逐行解释：

- 第 25 行：定义异步评价函数。
- 第 26 行：接收实例。
- 第 27 行：强制业务参数具名传入。
- 第 28 行：接收 subjectId。
- 第 29 行：接收简历原文。
- 第 30 行：接收目标岗位。
- 第 31 行：声明返回 ResumeEvaluation。
- 第 32 行：去除原文首尾空白。
- 第 33 行：检查规范文本是否为空。
- 第 34 行：为空时抛 ValueError。
- 第 36 行：调用 SkillRegistry.get("resume-analyst")。
- 第 37 行：调用 PromptLoader.render。
- 第 38 行：加载 resume/analysis.md 并注入 Skill 指令。
- 第 39 行：得到系统 Prompt。
- 第 40 行：开始构造输入。
- 第 41 行：放入 subjectId。
- 第 42 行：放入 targetRole。
- 第 43 行：放入清理后的 resumeText。
- 第 44 行：结束输入。
- 第 45 行：调用 StructuredOutputInvoker.invoke。
- 第 46 行：传入模型。
- 第 47 行：要求 ResumeEvaluation schema。
- 第 48 行：传入系统 Prompt。
- 第 49 行：传入输入字典。
- 第 50 行：返回通过校验的评价。

### 3.2 `python-agent/app/infrastructure/reliability/structured_output.py` 完整源码

~~~python
"""面向 OpenAI-compatible 模型的通用受控 JSON 输出能力。"""

import json
from collections.abc import Mapping
from typing import Any, Protocol, TypeVar

from langchain_core.messages import AIMessage, HumanMessage, SystemMessage
from pydantic import BaseModel, ValidationError

from app.common.exceptions import ModelOutputError
from app.common.prompt_loader import PromptLoader

from .retry import AsyncRetryExecutor


T = TypeVar("T", bound=BaseModel)


class RawChatModel(Protocol):
    async def ainvoke(self, input_value: object) -> object: ...


class StructuredOutputInvoker:
    """以提示词约束 + 本地校验替代供应商专有 Structured Outputs。"""

    def __init__(self, prompt_loader: PromptLoader, retry_executor: AsyncRetryExecutor | None) -> None:
        self._prompt_loader = prompt_loader
        self._retry_executor = retry_executor

    async def invoke(
        self,
        *,
        model: RawChatModel,
        schema: type[T],
        business_prompt: str,
        input_payload: Mapping[str, object],
    ) -> T:
        format_prompt = self._prompt_loader.render(
            "shared/structured-output.md",
            {
                "schema_json": json.dumps(schema.model_json_schema(by_alias=True), ensure_ascii=False),
                "few_shot_input": json.dumps({"task": "格式示例"}, ensure_ascii=False),
                "few_shot_output": json.dumps(_few_shot_output(schema), ensure_ascii=False),
            },
        )
        messages: list[object] = [
            SystemMessage(content=f"{business_prompt}\n\n{format_prompt}"),
            HumanMessage(content=json.dumps(input_payload, ensure_ascii=False, default=str)),
        ]
        max_corrections = self._retry_executor.max_output_correction_attempts if self._retry_executor else 0

        for correction_attempt in range(max_corrections + 1):
            raw_result = await self._invoke_model(model, messages)
            try:
                return self._validate(schema, raw_result)
            except (json.JSONDecodeError, ValidationError, TypeError, ValueError) as error:
                reason = _readable_validation_error(error)
                if correction_attempt == max_corrections:
                    raise ModelOutputError(
                        f"模型连续 {correction_attempt + 1} 次未返回符合 {schema.__name__} 的 JSON；"
                        f"最后一次原因：{reason}"
                    ) from error
                messages.extend([
                    AIMessage(content=_content_as_text(raw_result)),
                    HumanMessage(content=(
                        "你上一轮输出未通过程序校验。请只修复并返回完整 JSON，不能省略字段，"
                        f"不能输出解释或 Markdown。校验原因：{reason}"
                    )),
                ])
        raise AssertionError("unreachable")

    async def _invoke_model(self, model: RawChatModel, messages: object) -> object:
        if self._retry_executor is None:
            return await model.ainvoke(messages)
        return await self._retry_executor.execute(lambda: model.ainvoke(messages))

    def _validate(self, schema: type[T], raw_result: object) -> T:
        if isinstance(raw_result, schema):
            return raw_result
        content = _content_as_text(raw_result)
        payload = json.loads(_strip_json_fence(content))
        if not isinstance(payload, dict):
            raise TypeError("模型输出根节点必须是 JSON 对象")
        return schema.model_validate(payload)


def _content_as_text(raw_result: object) -> str:
    if isinstance(raw_result, str):
        return raw_result
    content = getattr(raw_result, "content", raw_result)
    if isinstance(content, str):
        return content
    if isinstance(content, list):
        fragments: list[str] = []
        for item in content:
            if isinstance(item, str):
                fragments.append(item)
            elif isinstance(item, Mapping) and isinstance(item.get("text"), str):
                fragments.append(item["text"])
        if fragments:
            return "".join(fragments)
    if isinstance(content, Mapping):
        return json.dumps(content, ensure_ascii=False)
    raise TypeError("模型响应不包含可解析的文本内容")


def _strip_json_fence(content: str) -> str:
    text = content.strip()
    if text.startswith("```") and text.endswith("```"):
        lines = text.splitlines()
        text = "\n".join(lines[1:-1]).strip()
    return text


def _readable_validation_error(error: BaseException) -> str:
    if isinstance(error, ValidationError):
        fields = [".".join(str(part) for part in item["loc"]) for item in error.errors()]
        return "字段校验失败：" + ", ".join(fields[:8])
    message = str(error).strip().replace("\n", " ")
    return message[:500] or error.__class__.__name__


def _few_shot_output(schema: type[BaseModel]) -> dict[str, Any]:
    """为每个实际结构提供合法的最小示例，避免格式提示停留在抽象层。"""
    examples: dict[str, dict[str, Any]] = {
        "ResumeEvaluation": {
            "overallScore": 75, "contentScore": 76, "structureScore": 78,
            "skillMatchScore": 74, "expressionScore": 72, "projectScore": 75,
            "summary": "示例：经历与目标岗位基本匹配。", "strengths": ["项目描述具体"],
            "suggestions": ["补充量化结果"], "issues": [{"question": "请说明项目职责", "priority": "MEDIUM", "suggestion": "补充职责边界"}],
            "technicalStack": ["Java"], "technicalDepth": ["有项目实践"], "careerPreferences": ["后端开发"],
        },
        "InterviewPlan": {
            "candidate_summary": "示例候选人", "strategy_summary": "按阶段考察", "selectedSkills": ["java-backend"],
            "stages": [
                {"stage": "OPENING", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["经历概述"], "time_budget_minutes": 3},
                {"stage": "PROJECT", "max_primary_questions": 2, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["项目"], "time_budget_minutes": 10},
                {"stage": "FUNDAMENTAL", "max_primary_questions": 2, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["基础"], "time_budget_minutes": 10},
                {"stage": "SCENARIO", "max_primary_questions": 1, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["场景"], "time_budget_minutes": 6},
                {"stage": "CODING", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["算法"], "time_budget_minutes": 8},
                {"stage": "SUMMARY", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["总结"], "time_budget_minutes": 3},
            ],
        },
        "InterviewSkillSelection": {"selectedSkills": ["interview-coach"]},
        "InterviewEvaluation": {"evaluation_summary": "回答覆盖了核心概念。", "score": 75, "answer_summary": "说明了基本原理。", "strengths": ["概念正确"], "weaknesses": ["缺少细节"], "preferences": []},
        "InterviewRoute": {"action": "NEXT_QUESTION", "next_topic": "线程池参数设计"},
        "GeneratedQuestion": {"question": "请说明线程池核心参数及其作用。"},
        "InterviewSummary": {"overallScore": 76, "summary": "整体基础扎实，工程细节可继续加强。", "strengths": ["基础概念准确"], "weaknesses": ["场景分析不足"], "suggestions": ["补充项目指标与取舍"],},
    }
    if schema.__name__ == "CrawlPageDecision":
        return {"pageType": "CONTENT", "includeAsKnowledge": True,
                "expandLinks": False, "relevanceScore": 85,
                "reason": "rich technical content", "selectedLinks": []}
    return examples.get(schema.__name__, {})
~~~

#### 3.2.1 `RawChatModel.ainvoke`

文件：`python-agent/app/infrastructure/reliability/structured_output.py:19-20`

逐行解释：

- 第 19 行：定义 `RawChatModel` Protocol；任何对象只要满足其方法签名即可注入，不要求继承具体 SDK 类。
- 第 20 行：声明异步 `ainvoke(input_value)` 边界，输入可为消息对象、输出为未知对象；行尾的 `...` 表示协议只描述接口，不在此发起请求。

#### 3.2.2 `StructuredOutputInvoker.__init__`

文件：`python-agent/app/infrastructure/reliability/structured_output.py:26-28`

逐行解释：

- 第 26 行：定义结构化输出器构造函数，接收 PromptLoader 和可选 AsyncRetryExecutor。
- 第 27 行：保存 PromptLoader，后续用它加载共享 JSON schema 约束模板。
- 第 28 行：保存重试执行器；为空时模型调用只执行一次，有值时经过统一超时、退避和有限重试。

#### 3.2.3 `StructuredOutputInvoker.invoke`、`_invoke_model`、`_validate` 与模块辅助函数

文件：`python-agent/app/infrastructure/reliability/structured_output.py:30-154`

`invoke` 文件：`python-agent/app/infrastructure/reliability/structured_output.py:30-70`

逐行解释：

- 第 30 行：代码为 `async def invoke(`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 31 行：代码为 `self,`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 32 行：代码为 `*,`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 33 行：代码为 `model: RawChatModel,`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 34 行：代码为 `schema: type[T],`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 35 行：代码为 `business_prompt: str,`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 36 行：代码为 `input_payload: Mapping[str, object],`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 37 行：代码为 `) -> T:`。本行在该函数中的具体作用是：定义通用结构化调用函数，输入模型、schema、业务 Prompt 和映射输入。
- 第 38 行：调用 PromptLoader.render 生成格式约束。
- 第 39 行：加载 shared/structured-output.md。
- 第 40 行：开始变量字典。
- 第 41 行：序列化 schema JSON。
- 第 42 行：加入固定 few-shot 输入。
- 第 43 行：调用 _few_shot_output 并序列化示例。
- 第 44 行：代码为 `},`。本行在该函数中的具体作用是：结束格式 Prompt。
- 第 45 行：代码为 `)`。本行在该函数中的具体作用是：结束格式 Prompt。
- 第 46 行：创建消息列表。
- 第 47 行：业务 Prompt 和格式约束组成系统消息。
- 第 48 行：业务输入序列化为用户消息。
- 第 49 行：结束消息列表。
- 第 50 行：读取最大输出纠错次数。
- 第 52 行：遍历初次和纠错尝试。
- 第 53 行：调用 _invoke_model。
- 第 54 行：进入校验保护。
- 第 55 行：调用 _validate，成功返回。
- 第 56 行：捕获 JSON、Pydantic、类型和值错误。
- 第 57 行：调用 _readable_validation_error。
- 第 58 行：检查次数耗尽。
- 第 59 行：构造 ModelOutputError。
- 第 60 行：代码为 `f"模型连续 {correction_attempt + 1} 次未返回符合 {schema.__name__} 的 JSON；"`。本行在该函数中的具体作用是：消息包含次数、schema 和最后原因。
- 第 61 行：代码为 `f"最后一次原因：{reason}"`。本行在该函数中的具体作用是：消息包含次数、schema 和最后原因。
- 第 62 行：从原错误抛出。
- 第 63 行：仍可纠错时扩展消息。
- 第 64 行：调用 _content_as_text 保存上一轮错误输出。
- 第 65 行：开始纠错用户消息。
- 第 66 行：代码为 `"你上一轮输出未通过程序校验。请只修复并返回完整 JSON，不能省略字段，"`。本行在该函数中的具体作用是：要求只返回完整 JSON，并附校验原因。
- 第 67 行：代码为 `f"不能输出解释或 Markdown。校验原因：{reason}"`。本行在该函数中的具体作用是：要求只返回完整 JSON，并附校验原因。
- 第 68 行：代码为 `)),`。本行在该函数中的具体作用是：结束扩展。
- 第 69 行：代码为 `])`。本行在该函数中的具体作用是：结束扩展。
- 第 70 行：理论不可达保护。

`_invoke_model`（`python-agent/app/infrastructure/reliability/structured_output.py:72-75`）逐行解释：

- 第 72 行：定义原始模型调用。
- 第 73 行：检查无重试器。
- 第 74 行：无重试时直接 ainvoke。
- 第 75 行：有重试时调用 AsyncRetryExecutor.execute。

`_validate`（`python-agent/app/infrastructure/reliability/structured_output.py:77-84`）逐行解释：

- 第 77 行：定义结构验证。
- 第 78 行：原结果已是 schema 实例时命中。
- 第 79 行：直接返回。
- 第 80 行：调用 _content_as_text。
- 第 81 行：清理围栏并解析 JSON。
- 第 82 行：检查根节点字典。
- 第 83 行：否则抛 TypeError。
- 第 84 行：调用 schema.model_validate。

`_content_as_text`（`python-agent/app/infrastructure/reliability/structured_output.py:87-104`）逐行解释：

- 第 87 行：定义文本提取。
- 第 88 行：代码为 `if isinstance(raw_result, str):`。本行在该函数中的具体作用是：原结果为字符串时返回。
- 第 89 行：代码为 `return raw_result`。本行在该函数中的具体作用是：原结果为字符串时返回。
- 第 90 行：读取 content 属性或原对象。
- 第 91 行：代码为 `if isinstance(content, str):`。本行在该函数中的具体作用是：content 为字符串时返回。
- 第 92 行：代码为 `return content`。本行在该函数中的具体作用是：content 为字符串时返回。
- 第 93 行：列表时进入块提取。
- 第 94 行：创建片段列表。
- 第 95 行：遍历块。
- 第 96 行：代码为 `if isinstance(item, str):`。本行在该函数中的具体作用是：字符串块直接加入。
- 第 97 行：代码为 `fragments.append(item)`。本行在该函数中的具体作用是：字符串块直接加入。
- 第 98 行：代码为 `elif isinstance(item, Mapping) and isinstance(item.get("text"), str):`。本行在该函数中的具体作用是：映射块 text 为字符串时加入。
- 第 99 行：代码为 `fragments.append(item["text"])`。本行在该函数中的具体作用是：映射块 text 为字符串时加入。
- 第 100 行：代码为 `if fragments:`。本行在该函数中的具体作用是：有片段时连接返回。
- 第 101 行：代码为 `return "".join(fragments)`。本行在该函数中的具体作用是：有片段时连接返回。
- 第 102 行：代码为 `if isinstance(content, Mapping):`。本行在该函数中的具体作用是：content 为映射时序列化返回。
- 第 103 行：代码为 `return json.dumps(content, ensure_ascii=False)`。本行在该函数中的具体作用是：content 为映射时序列化返回。
- 第 104 行：其他形态抛 TypeError。

`_strip_json_fence`（`python-agent/app/infrastructure/reliability/structured_output.py:107-112`）逐行解释：

- 第 107 行：定义围栏清理。
- 第 108 行：去首尾空白。
- 第 109 行：检查三反引号包围。
- 第 110 行：按行拆分。
- 第 111 行：去围栏行并连接。
- 第 112 行：返回文本。

`_readable_validation_error`（`python-agent/app/infrastructure/reliability/structured_output.py:115-120`）逐行解释：

- 第 115 行：定义可读错误。
- 第 116 行：识别 ValidationError。
- 第 117 行：提取字段路径。
- 第 118 行：最多前 8 项。
- 第 119 行：其他错误转单行字符串。
- 第 120 行：最多 500 字符，空时类名。

`_few_shot_output`（`python-agent/app/infrastructure/reliability/structured_output.py:123-154`）逐行解释：

- 第 123 行：定义 schema 示例函数。
- 第 124 行：说明提供实际合法示例。
- 第 125 行：代码为 `examples: dict[str, dict[str, Any]] = {`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 126 行：代码为 `"ResumeEvaluation": {`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 127 行：代码为 `"overallScore": 75, "contentScore": 76, "structureScore": 78,`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 128 行：代码为 `"skillMatchScore": 74, "expressionScore": 72, "projectScore": 75,`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 129 行：代码为 `"summary": "示例：经历与目标岗位基本匹配。", "strengths": ["项目描述具体"],`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 130 行：代码为 `"suggestions": ["补充量化结果"], "issues": [{"question": "请说明项目职责", "priority": "MEDIUM", "suggestion": "补充职责边界"}],`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 131 行：代码为 `"technicalStack": ["Java"], "technicalDepth": ["有项目实践"], "careerPreferences": ["后端开发"],`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 132 行：代码为 `},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 133 行：代码为 `"InterviewPlan": {`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 134 行：代码为 `"candidate_summary": "示例候选人", "strategy_summary": "按阶段考察", "selectedSkills": ["java-backend"],`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 135 行：代码为 `"stages": [`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 136 行：代码为 `{"stage": "OPENING", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["经历概述"], "time_budget_minutes": 3},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 137 行：代码为 `{"stage": "PROJECT", "max_primary_questions": 2, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["项目"], "time_budget_minutes": 10},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 138 行：代码为 `{"stage": "FUNDAMENTAL", "max_primary_questions": 2, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["基础"], "time_budget_minutes": 10},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 139 行：代码为 `{"stage": "SCENARIO", "max_primary_questions": 1, "max_followups_per_question": 1, "difficulty": "MEDIUM", "topics": ["场景"], "time_budget_minutes": 6},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 140 行：代码为 `{"stage": "CODING", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["算法"], "time_budget_minutes": 8},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 141 行：代码为 `{"stage": "SUMMARY", "max_primary_questions": 1, "max_followups_per_question": 0, "difficulty": "MEDIUM", "topics": ["总结"], "time_budget_minutes": 3},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 142 行：代码为 `],`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 143 行：代码为 `},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 144 行：代码为 `"InterviewSkillSelection": {"selectedSkills": ["interview-coach"]},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 145 行：代码为 `"InterviewEvaluation": {"evaluation_summary": "回答覆盖了核心概念。", "score": 75, "answer_summary": "说明了基本原理。", "strengths": ["概念正确"], "weaknesses": ["缺少细节"], "preferences": []},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 146 行：代码为 `"InterviewRoute": {"action": "NEXT_QUESTION", "next_topic": "线程池参数设计"},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 147 行：代码为 `"GeneratedQuestion": {"question": "请说明线程池核心参数及其作用。"},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 148 行：代码为 `"InterviewSummary": {"overallScore": 76, "summary": "整体基础扎实，工程细节可继续加强。", "strengths": ["基础概念准确"], "weaknesses": ["场景分析不足"], "suggestions": ["补充项目指标与取舍"],},`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 149 行：代码为 `}`。本行在该函数中的具体作用是：建立 schema 示例映射；本接口使用 ResumeEvaluation 项。
- 第 150 行：代码为 `if schema.__name__ == "CrawlPageDecision":`。本行在该函数中的具体作用是：爬取判断专用示例，本接口不触发。
- 第 151 行：代码为 `return {"pageType": "CONTENT", "includeAsKnowledge": True,`。本行在该函数中的具体作用是：爬取判断专用示例，本接口不触发。
- 第 152 行：代码为 `"expandLinks": False, "relevanceScore": 85,`。本行在该函数中的具体作用是：爬取判断专用示例，本接口不触发。
- 第 153 行：代码为 `"reason": "rich technical content", "selectedLinks": []}`。本行在该函数中的具体作用是：爬取判断专用示例，本接口不触发。
- 第 154 行：按 schema 名返回，未知时空字典。

### 3.3 `python-agent/app/infrastructure/reliability/retry.py` 完整源码

~~~python
"""统一异步调用重试执行器。"""

import asyncio
from collections.abc import Awaitable, Callable
from typing import TypeVar

from app.common.exceptions import AgentDependencyError

from .policy import RetryPolicy


T = TypeVar("T")


class AsyncRetryExecutor:
    def __init__(self, policy: RetryPolicy) -> None:
        self._policy = policy

    @property
    def max_output_correction_attempts(self) -> int:
        return self._policy.max_output_correction_attempts

    async def execute(self, operation: Callable[[], Awaitable[T]]) -> T:
        for attempt in range(1, self._policy.max_attempts + 1):
            try:
                # 该上限对所有经由此执行器的模型与外部 Agent 调用生效；
                # wait_for 取消超时协程，避免请求在后台无限悬挂。
                return await asyncio.wait_for(
                    operation(), timeout=self._policy.attempt_timeout_seconds
                )
            except Exception as error:
                if not self._is_retryable(error) or attempt == self._policy.max_attempts:
                    if self._is_retryable(error):
                        raise AgentDependencyError(
                            "模型或外部 Agent 依赖在有限重试后仍不可用",
                            retryable=True,
                        ) from error
                    raise
                await asyncio.sleep(self._backoff_seconds(attempt))
        raise AssertionError("unreachable")

    def _is_retryable(self, error: BaseException) -> bool:
        return type(error).__name__ in self._policy.retryable_errors

    def _backoff_seconds(self, attempt: int) -> float:
        milliseconds = min(
            self._policy.max_backoff_milliseconds,
            self._policy.initial_backoff_milliseconds * (2 ** (attempt - 1)),
        )
        return milliseconds / 1000
~~~

#### 3.3.1 `AsyncRetryExecutor.__init__`

文件：`python-agent/app/infrastructure/reliability/retry.py:16-17`

逐行解释：

- 第 16 行：定义重试执行器构造函数，接收已经加载并校验的 RetryPolicy。
- 第 17 行：保存策略；最大次数、单次超时、退避上限、可重试异常名和输出纠错次数都从该对象读取。

#### 3.3.2 `max_output_correction_attempts`

文件：`python-agent/app/infrastructure/reliability/retry.py:19-21`

逐行解释：

- 第 19 行：`@property` 允许调用方像读取只读字段一样取得纠错次数。
- 第 20 行：定义属性访问函数并声明返回整数。
- 第 21 行：返回策略中的 `max_output_correction_attempts`；该次数控制格式纠错轮数，不等同于网络重试次数。

#### 3.3.3 `AsyncRetryExecutor.execute`、`_is_retryable` 与 `_backoff_seconds`

文件：`python-agent/app/infrastructure/reliability/retry.py:23-50`

逐行解释：

- 第 23 行：定义异步重试函数。
- 第 24 行：尝试编号从 1 到最大次数。
- 第 25 行：进入单次保护。
- 第 26 行：代码为 `# 该上限对所有经由此执行器的模型与外部 Agent 调用生效；`。本行在该函数中的具体作用是：注释说明超时取消协程。
- 第 27 行：代码为 `# wait_for 取消超时协程，避免请求在后台无限悬挂。`。本行在该函数中的具体作用是：注释说明超时取消协程。
- 第 28 行：调用 asyncio.wait_for。
- 第 29 行：创建操作协程并用单次超时。
- 第 30 行：成功返回。
- 第 31 行：捕获异常。
- 第 32 行：不可重试或最后一次时停止。
- 第 33 行：区分可重试次数耗尽。
- 第 34 行：代码为 `raise AgentDependencyError(`。本行在该函数中的具体作用是：构造可重试 AgentDependencyError 并链接原异常。
- 第 35 行：代码为 `"模型或外部 Agent 依赖在有限重试后仍不可用",`。本行在该函数中的具体作用是：构造可重试 AgentDependencyError 并链接原异常。
- 第 36 行：代码为 `retryable=True,`。本行在该函数中的具体作用是：构造可重试 AgentDependencyError 并链接原异常。
- 第 37 行：代码为 `) from error`。本行在该函数中的具体作用是：构造可重试 AgentDependencyError 并链接原异常。
- 第 38 行：不可重试错误原样抛出。
- 第 39 行：调用 _backoff_seconds 并异步等待。
- 第 40 行：理论不可达保护。
- 第 42 行：定义 _is_retryable。
- 第 43 行：按异常类名匹配策略集合。
- 第 45 行：定义退避计算。
- 第 46 行：代码为 `milliseconds = min(`。本行在该函数中的具体作用是：取最大退避与指数退避较小值。
- 第 47 行：代码为 `self._policy.max_backoff_milliseconds,`。本行在该函数中的具体作用是：取最大退避与指数退避较小值。
- 第 48 行：代码为 `self._policy.initial_backoff_milliseconds * (2 ** (attempt - 1)),`。本行在该函数中的具体作用是：取最大退避与指数退避较小值。
- 第 49 行：代码为 `)`。本行在该函数中的具体作用是：取最大退避与指数退避较小值。
- 第 50 行：毫秒转秒。

### 3.4 `python-agent/app/common/prompt_loader.py` 完整源码

~~~python
"""外部 Prompt 文件加载与受控变量渲染。"""

import re
from pathlib import Path
from typing import Mapping

from .config import PROJECT_DIR
from .exceptions import PromptConfigurationError


class PromptLoader:
    """从 resources/prompts 读取 Prompt，禁止在业务代码中维护可修改模板。"""

    _placeholder_pattern = re.compile(r"{{\s*([a-zA-Z_][a-zA-Z0-9_]*)\s*}}")

    def __init__(self, root: Path | None = None) -> None:
        self._root = root or PROJECT_DIR / "resources" / "prompts"

    def load(self, prompt_id: str) -> str:
        path = self._resolve(prompt_id)
        try:
            return path.read_text(encoding="utf-8")
        except FileNotFoundError as error:
            raise PromptConfigurationError(f"Prompt 文件不存在: {prompt_id}") from error

    def render(self, prompt_id: str, variables: Mapping[str, object]) -> str:
        template = self.load(prompt_id)

        def replace(match: re.Match[str]) -> str:
            key = match.group(1)
            if key not in variables:
                raise PromptConfigurationError(
                    f"Prompt {prompt_id} 缺少变量: {key}"
                )
            return str(variables[key])

        rendered = self._placeholder_pattern.sub(replace, template)
        if self._placeholder_pattern.search(rendered):
            raise PromptConfigurationError(f"Prompt {prompt_id} 存在未替换变量")
        return rendered

    def _resolve(self, prompt_id: str) -> Path:
        path = (self._root / prompt_id).resolve()
        if self._root.resolve() not in path.parents:
            raise PromptConfigurationError("Prompt 路径不允许越过配置目录")
        return path
~~~

#### 3.4.1 `PromptLoader.__init__`

文件：`python-agent/app/common/prompt_loader.py:16-17`

逐行解释：

- 第 16 行：定义 PromptLoader 构造函数，允许测试或特殊装配显式传入根目录。
- 第 17 行：优先使用显式根目录；未传入时固定到项目 `resources/prompts`，避免业务代码散落模板路径。

#### 3.4.2 `PromptLoader.render`、`load` 与 `_resolve`

文件：`python-agent/app/common/prompt_loader.py:19-46`

`render` 逐行解释：

- 第 26 行：定义渲染函数。
- 第 27 行：调用 load 读取模板。
- 第 29 行：定义闭包 replace。
- 第 30 行：读取占位符键。
- 第 31 行：检查变量字典。
- 第 32 行：代码为 `raise PromptConfigurationError(`。本行在该函数中的具体作用是：缺失时抛包含 Prompt ID 和键的配置错误。
- 第 33 行：代码为 `f"Prompt {prompt_id} 缺少变量: {key}"`。本行在该函数中的具体作用是：缺失时抛包含 Prompt ID 和键的配置错误。
- 第 34 行：代码为 `)`。本行在该函数中的具体作用是：缺失时抛包含 Prompt ID 和键的配置错误。
- 第 35 行：存在时转字符串。
- 第 37 行：替换全部占位符。
- 第 38 行：再次检查残留。
- 第 39 行：残留时抛错。
- 第 40 行：返回渲染文本。

`load` 逐行解释：

- 第 19 行：定义加载函数。
- 第 20 行：调用 _resolve。
- 第 21 行：进入读取保护。
- 第 22 行：UTF-8 读取。
- 第 23 行：捕获文件不存在。
- 第 24 行：转换为 PromptConfigurationError。

`_resolve` 逐行解释：

- 第 42 行：定义安全路径解析。
- 第 43 行：拼接根目录后 resolve。
- 第 44 行：检查根目录仍是父目录。
- 第 45 行：越界抛错。
- 第 46 行：返回路径。

#### 3.4.3 `PromptLoader.render.<locals>.replace`

文件：`python-agent/app/common/prompt_loader.py:29-35`

逐行解释：

- 第 29 行：在 `render` 内定义替换回调；正则每匹配一个占位符就调用一次。
- 第 30 行：读取正则捕获组中的变量名。
- 第 31 行：检查调用方变量映射是否包含该键。
- 第 32 行：缺键时开始构造 PromptConfigurationError。
- 第 33 行：错误文本包含 Prompt ID 和缺失变量名，便于定位配置。
- 第 34 行：闭合异常构造并抛出。
- 第 35 行：变量存在时转成字符串并返回给正则替换器。

## 4. 主流构建分析

当前实现使用“通用 Prompt 格式约束 + 本地 JSON 解析 + Pydantic 校验 + 有限格式纠错”来兼容所有 OpenAI-compatible 模型。优点是没有绑定供应商专有 Structured Outputs API，同一套业务 Agent 可以切换兼容端点；格式失败原因会被压缩后反馈给模型，网络重试与输出纠错也被明确区分。缺点是 schema、few-shot 示例和业务 Prompt 最终仍以文本传给模型，不能像原生 JSON Schema 解码那样从生成阶段保证合法；`_few_shot_output` 通过 schema 名维护示例，新增模型时容易遗漏，且纠错会增加 token 与延迟。

主流实现通常优先使用模型供应商原生的 JSON Schema/工具调用能力，并把纯文本解析作为兼容回退。原生能力的优点是生成约束更强、无效 JSON 概率更低、纠错轮次减少；缺点是不同供应商对 schema 关键字、工具调用和错误格式的支持不一致，会削弱本项目当前的 provider 可替换性。另一个可选方向是使用 Instructor 等 schema 适配库，优点是减少手写解析代码，缺点是增加第三方运行时依赖，且其重试策略可能与现有 AsyncRetryExecutor 叠加。

本项目适合采用“能力检测 + 双通道”方式：保留当前 `StructuredOutputInvoker` 作为统一入口，在 `RawChatModel` 适配层声明是否支持原生 schema；支持时调用原生 `with_structured_output` 或工具调用并继续执行 `schema.model_validate`，不支持时走当前 Prompt、`_content_as_text` 与 `_strip_json_fence`。实施时必须保持 `AsyncRetryExecutor` 只负责传输或依赖错误，输出纠错只由结构化输出器负责，禁用 SDK 内部重复重试；同时把 `_few_shot_output` 改为 schema 类上的示例元数据或独立注册表，在启动测试中逐个执行 `model_validate`，防止示例与模型字段漂移。
