# Python 装配与持久化：完整源码逐函数解析

## 1. 接口定义

本章说明 Python 的应用装配、异步数据库连接与仓储实现。它们由会话初始化、回答、暂停/完成、简历评价、RAG 索引、删除和检索接口间接调用：PostgreSQL 是会话、记忆和向量数据的最终真相源；Redis 仅缓存面试会话读取结果。

## 2. 函数调用链

```text
FastAPI resolver / lifespan
  -> build_interview_agent_service
     -> build_cache -> RedisCache
     -> create_engine -> create_session_factory
     -> PostgresInterviewSessionRepository / PostgresLongTermMemoryRepository
     -> build_memory_service -> build_rag_service -> PostgresRagVectorRepository
     -> build_resume_evaluation_agent
会话创建 -> InterviewSessionRepository.create -> _to_entity -> commit -> refresh -> _from_entity -> _cache_session
会话读取 -> InterviewSessionRepository.get -> RedisCache.get_json（命中）或 select -> _from_entity -> _cache_session
会话保存 -> InterviewSessionRepository.save -> update(stateVersion 条件) -> commit / rollback -> _from_entity -> _cache_session
长期记忆 -> LongTermMemoryRepository.get / create / save -> _to_entity -> select / insert / update -> commit / rollback
RAG 索引 -> RagVectorRepository.replace_for_knowledge_base -> delete_by_knowledge_base -> add -> _to_entity -> commit
RAG 搜索 -> RagVectorRepository.search -> select(向量距离排序) -> _from_entity
```

## 3. 函数解析

### 3.1 `python-agent/app/bootstrap.py` 完整源码

~~~python
"""生产环境的下层 Agent 服务依赖组装。"""

from app.agents.interview.agent import InterviewEvaluationAgent, InterviewPlanner, InterviewQuestionAgent, InterviewRoutingAgent, InterviewSummaryAgent
from app.agents.evaluation.agent import ResumeEvaluationAgent
from app.agents.interview.service import InterviewAgentService
from app.agents.interview.workflow import InterviewWorkflow
from app.agents.llm.factory import LLMFactory
from app.memory.policy import MemoryPolicy
from app.memory.service import MemoryService
from app.rag.embedding import OpenAIEmbeddingProvider
from app.rag.policy import RagPolicy
from app.rag.service import RagSearchTool, RagService
from app.tools.web_search import WebEvidenceTool
from app.infrastructure.reliability.policy import RetryPolicy
from app.infrastructure.reliability.retry import AsyncRetryExecutor
from app.infrastructure.idempotency.policy import IdempotencyPolicy
from app.tools.skills.loader import SkillRegistry
from app.common.config import Settings, get_settings
from app.common.prompt_loader import PromptLoader
from app.infrastructure.persistence.database import create_session_factory
from app.infrastructure.persistence.interview_session_repository import (
    PostgresInterviewSessionRepository,
)
from app.infrastructure.persistence.long_term_memory_repository import (
    PostgresLongTermMemoryRepository,
)
from app.infrastructure.persistence.rag_vector_repository import (
    PostgresRagVectorRepository,
)
from app.infrastructure.cache.redis_cache import RedisCache


def build_memory_service(settings: Settings | None = None) -> MemoryService:
    current = settings or get_settings()
    session_factory = create_session_factory(current)
    return MemoryService(
        PostgresLongTermMemoryRepository(session_factory), MemoryPolicy.load()
    )


def build_cache(settings: Settings | None = None) -> RedisCache:
    current = settings or get_settings()
    return RedisCache(current.redis_url)

def build_interview_agent_service(
    settings: Settings | None = None,
) -> InterviewAgentService:
    """建立真实依赖；不自动建表，也不允许在无数据库时退化为临时文件。"""

    current = settings or get_settings()
    session_factory = create_session_factory(current)
    prompt_loader = PromptLoader()
    skill_registry = SkillRegistry()
    workflow = InterviewWorkflow.load(prompt_loader)
    model = LLMFactory.create_chat_model(current)
    retry_executor = AsyncRetryExecutor(RetryPolicy.load())
    rag_tool = RagSearchTool(build_rag_service(current)) if current.embedding_model else None

    return InterviewAgentService(
        planner=InterviewPlanner(
            model, prompt_loader, skill_registry, retry_executor
        ),
        evaluation_agent=InterviewEvaluationAgent(
            model, prompt_loader, skill_registry, retry_executor
        ),
        routing_agent=InterviewRoutingAgent(
            model, prompt_loader, skill_registry, retry_executor
        ),
        question_agent=InterviewQuestionAgent(model, prompt_loader, skill_registry, retry_executor),
        rag_tool=rag_tool,
        repository=PostgresInterviewSessionRepository(session_factory, build_cache(current)),
        workflow=workflow,
        prompt_loader=prompt_loader,
        memory_service=build_memory_service(current),
        summary_agent=InterviewSummaryAgent(model, prompt_loader, retry_executor),
        idempotency_policy=IdempotencyPolicy.load(),
        web_evidence_tool=WebEvidenceTool(),
        cache=build_cache(current),
    )


def build_rag_service(settings: Settings | None = None) -> RagService:
    current = settings or get_settings()
    session_factory = create_session_factory(current)
    retry_executor = AsyncRetryExecutor(RetryPolicy.load())
    return RagService(
        repository=PostgresRagVectorRepository(session_factory),
        embedding_provider=OpenAIEmbeddingProvider(current, retry_executor),
        policy=RagPolicy.load(),
        cache=build_cache(current),
    )


def build_resume_evaluation_agent(
    settings: Settings | None = None,
) -> ResumeEvaluationAgent:
    current = settings or get_settings()
    retry_executor = AsyncRetryExecutor(RetryPolicy.load())
    return ResumeEvaluationAgent(
        model=LLMFactory.create_chat_model(current),
        prompt_loader=PromptLoader(),
        skill_registry=SkillRegistry(),
        retry_executor=retry_executor,
    )
~~~

#### 3.1.1 `build_memory_service`

文件：`python-agent/app/bootstrap.py:33-40`

功能：装配长期记忆服务。

逐行解释：

- 第 33 行：代码为 `def build_memory_service(settings: Settings | None = None) -> MemoryService:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 34 行：代码为 `current = settings or get_settings()`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 35 行：代码为 `session_factory = create_session_factory(current)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 36 行：代码为 `return MemoryService(`。结束当前分支或函数并返回结果。
- 第 37 行：代码为 `PostgresLongTermMemoryRepository(session_factory), MemoryPolicy.load()`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 38 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.1.2 `build_cache`

文件：`python-agent/app/bootstrap.py:41-44`

功能：按配置创建 Python Redis 缓存。

逐行解释：

- 第 41 行：代码为 `def build_cache(settings: Settings | None = None) -> RedisCache:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 42 行：代码为 `current = settings or get_settings()`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 43 行：代码为 `return RedisCache(current.redis_url)`。结束当前分支或函数并返回结果。

#### 3.1.3 `build_interview_agent_service`

文件：`python-agent/app/bootstrap.py:45-46`

功能：装配面试服务的所有依赖。

逐行解释：

- 第 45 行：代码为 `def build_interview_agent_service(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 46 行：代码为 `settings: Settings | None = None,`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.1.4 `build_rag_service`

文件：`python-agent/app/bootstrap.py:82-93`

功能：装配 RAG 服务与向量仓储。

逐行解释：

- 第 82 行：代码为 `def build_rag_service(settings: Settings | None = None) -> RagService:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 83 行：代码为 `current = settings or get_settings()`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 84 行：代码为 `session_factory = create_session_factory(current)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 85 行：代码为 `retry_executor = AsyncRetryExecutor(RetryPolicy.load())`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 86 行：代码为 `return RagService(`。结束当前分支或函数并返回结果。
- 第 87 行：代码为 `repository=PostgresRagVectorRepository(session_factory),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 88 行：代码为 `embedding_provider=OpenAIEmbeddingProvider(current, retry_executor),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 89 行：代码为 `policy=RagPolicy.load(),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 90 行：代码为 `cache=build_cache(current),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 91 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.1.5 `build_resume_evaluation_agent`

文件：`python-agent/app/bootstrap.py:94-95`

功能：装配简历评价 Agent。

逐行解释：

- 第 94 行：代码为 `def build_resume_evaluation_agent(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 95 行：代码为 `settings: Settings | None = None,`。这一行计算、保存或传递当前步骤所需的数据。

### 3.2 `python-agent/app/infrastructure/persistence/database.py` 完整源码

~~~python
"""PostgreSQL 异步连接与开发期建表入口。"""

from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker, create_async_engine

from app.common.config import Settings, get_settings
from app.common.exceptions import PersistenceConfigurationError


def create_engine(settings: Settings | None = None) -> AsyncEngine:
    current = settings or get_settings()
    if not current.database_url:
        raise PersistenceConfigurationError("DATABASE_URL 未配置")
    return create_async_engine(current.database_url, pool_pre_ping=True)


def create_session_factory(
    settings: Settings | None = None,
) -> async_sessionmaker[AsyncSession]:
    return async_sessionmaker(create_engine(settings), expire_on_commit=False)


async def create_schema(engine: AsyncEngine) -> None:
    """创建 Python Agent 自有表；基础设施升级由版本化 SQL 脚本负责。"""

    from .interview_session_repository import Base
    from .long_term_memory_repository import LongTermMemoryEntity
    from .rag_vector_repository import RagChunkEntity

    async with engine.begin() as connection:
        await connection.run_sync(Base.metadata.create_all)
~~~

#### 3.2.1 `create_engine`

文件：`python-agent/app/infrastructure/persistence/database.py:9-15`

功能：创建 SQLAlchemy 异步数据库引擎。

逐行解释：

- 第 9 行：代码为 `def create_engine(settings: Settings | None = None) -> AsyncEngine:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 10 行：代码为 `current = settings or get_settings()`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 11 行：代码为 `if not current.database_url:`。开始条件分支，决定是否走缓存、校验、异常或降级路径。
- 第 12 行：代码为 `raise PersistenceConfigurationError("DATABASE_URL 未配置")`。抛出异常，把失败明确传给上层。
- 第 13 行：代码为 `return create_async_engine(current.database_url, pool_pre_ping=True)`。结束当前分支或函数并返回结果。

#### 3.2.2 `create_session_factory`

文件：`python-agent/app/infrastructure/persistence/database.py:16-17`

功能：创建异步会话工厂。

逐行解释：

- 第 16 行：代码为 `def create_session_factory(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 17 行：代码为 `settings: Settings | None = None,`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.2.3 `create_schema`

文件：`python-agent/app/infrastructure/persistence/database.py:22-30`

功能：创建 ORM 表结构。

逐行解释：

- 第 22 行：代码为 `async def create_schema(engine: AsyncEngine) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 23 行：代码为 `"""创建 Python Agent 自有表；基础设施升级由版本化 SQL 脚本负责。"""`。这一行计算、保存或传递当前步骤所需的数据。
- 第 25 行：代码为 `from .interview_session_repository import Base`。这一行计算、保存或传递当前步骤所需的数据。
- 第 26 行：代码为 `from .long_term_memory_repository import LongTermMemoryEntity`。这一行计算、保存或传递当前步骤所需的数据。
- 第 27 行：代码为 `from .rag_vector_repository import RagChunkEntity`。这一行计算、保存或传递当前步骤所需的数据。
- 第 29 行：代码为 `async with engine.begin() as connection:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 30 行：代码为 `await connection.run_sync(Base.metadata.create_all)`。等待异步 I/O 完成，不阻塞事件循环。

### 3.3 `python-agent/app/infrastructure/persistence/interview_session_repository.py` 完整源码

~~~python
"""基于 PostgreSQL 的 Agent 面试会话持久化。"""

from datetime import datetime, timezone

from sqlalchemy import DateTime, Integer, JSON, String, select, update
from sqlalchemy.exc import IntegrityError
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column

from app.agents.interview.models import InterviewSession
from app.agents.interview.repository import InterviewSessionRepository
from app.common.exceptions import ConsistencyError
from app.infrastructure.cache.redis_cache import RedisCache


class Base(DeclarativeBase):
    pass


class InterviewSessionEntity(Base):
    __tablename__ = "agent_interview_sessions"

    session_id: Mapped[str] = mapped_column(String(128), primary_key=True)
    user_id: Mapped[str] = mapped_column(String(128), nullable=False, index=True)
    status: Mapped[str] = mapped_column(String(32), nullable=False)
    current_stage: Mapped[str] = mapped_column(String(32), nullable=False)
    state_version: Mapped[int] = mapped_column(Integer, nullable=False)
    session_data: Mapped[dict] = mapped_column(JSON, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)


class PostgresInterviewSessionRepository(InterviewSessionRepository):
    def __init__(self, session_factory: async_sessionmaker[AsyncSession], cache: RedisCache | None = None) -> None:
        self._session_factory = session_factory
        self._cache = cache

    async def create(self, session: InterviewSession) -> InterviewSession:
        entity = self._to_entity(session)
        try:
            async with self._session_factory() as db_session:
                db_session.add(entity)
                await db_session.commit()
        except IntegrityError as error:
            raise ConsistencyError("Agent 会话已存在") from error
        await self._cache_session(session)
        return session

    async def get(self, session_id: str) -> InterviewSession | None:
        cached = await self._cache.get_json(self._cache_key(session_id)) if self._cache else None
        if isinstance(cached, dict):
            try:
                return InterviewSession.model_validate(cached)
            except ValueError:
                await self._cache.delete(self._cache_key(session_id))
        async with self._session_factory() as db_session:
            entity = await db_session.scalar(
                select(InterviewSessionEntity).where(
                    InterviewSessionEntity.session_id == session_id
                )
            )
        session = self._from_entity(entity) if entity is not None else None
        if session is not None:
            await self._cache_session(session)
        return session

    async def save(
        self, session: InterviewSession, *, expected_version: int
    ) -> InterviewSession:
        next_version = expected_version + 1
        saved_session = session.model_copy(
            update={
                "state_version": next_version,
                "updated_at": datetime.now(timezone.utc),
            }
        )
        payload = saved_session.model_dump(mode="json")
        statement = (
            update(InterviewSessionEntity)
            .where(
                InterviewSessionEntity.session_id == saved_session.session_id,
                InterviewSessionEntity.state_version == expected_version,
            )
            .values(
                status=saved_session.status,
                current_stage=saved_session.current_stage,
                state_version=next_version,
                session_data=payload,
                updated_at=saved_session.updated_at,
            )
        )
        async with self._session_factory() as db_session:
            result = await db_session.execute(statement)
            if result.rowcount != 1:
                await db_session.rollback()
                if self._cache:
                    await self._cache.delete(self._cache_key(session.session_id))
                raise ConsistencyError("Agent 会话状态已被并发修改")
            await db_session.commit()
        # PostgreSQL optimistic-lock commit succeeds before the cache is
        # changed, so a cache outage cannot manufacture a session version.
        await self._cache_session(saved_session)
        return saved_session

    @staticmethod
    def _cache_key(session_id: str) -> str:
        return f"python:interview-session:{session_id}"

    async def _cache_session(self, session: InterviewSession) -> None:
        if self._cache:
            await self._cache.set_json(
                self._cache_key(session.session_id), session.model_dump(mode="json"), ttl_seconds=7200
            )

    @staticmethod
    def _to_entity(session: InterviewSession) -> InterviewSessionEntity:
        return InterviewSessionEntity(
            session_id=session.session_id,
            user_id=session.user_id,
            status=session.status,
            current_stage=session.current_stage,
            state_version=session.state_version,
            session_data=session.model_dump(mode="json"),
            created_at=session.created_at,
            updated_at=session.updated_at,
        )

    @staticmethod
    def _from_entity(entity: InterviewSessionEntity) -> InterviewSession:
        return InterviewSession.model_validate(entity.session_data)
~~~

#### 3.3.1 `PostgresInterviewSessionRepository.__init__`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:34-37`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 34 行：代码为 `def __init__(self, session_factory: async_sessionmaker[AsyncSession], cache: RedisCache | None = None) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 35 行：代码为 `self._session_factory = session_factory`。这一行计算、保存或传递当前步骤所需的数据。
- 第 36 行：代码为 `self._cache = cache`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.3.2 `PostgresInterviewSessionRepository.create`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:38-48`

功能：创建会话并回填缓存。

逐行解释：

- 第 38 行：代码为 `async def create(self, session: InterviewSession) -> InterviewSession:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 39 行：代码为 `entity = self._to_entity(session)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 40 行：代码为 `try:`。开始异常保护块。
- 第 41 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 42 行：代码为 `db_session.add(entity)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 43 行：代码为 `await db_session.commit()`。等待异步 I/O 完成，不阻塞事件循环。
- 第 44 行：代码为 `except IntegrityError as error:`。捕获指定异常并进入项目定义的回滚或转换路径。
- 第 45 行：代码为 `raise ConsistencyError("Agent 会话已存在") from error`。抛出异常，把失败明确传给上层。
- 第 46 行：代码为 `await self._cache_session(session)`。等待异步 I/O 完成，不阻塞事件循环。
- 第 47 行：代码为 `return session`。结束当前分支或函数并返回结果。

#### 3.3.3 `PostgresInterviewSessionRepository.get`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:49-66`

功能：读取会话，缓存优先。

逐行解释：

- 第 49 行：代码为 `async def get(self, session_id: str) -> InterviewSession | None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 50 行：代码为 `cached = await self._cache.get_json(self._cache_key(session_id)) if self._cache else None`。这一行计算、保存或传递当前步骤所需的数据。
- 第 51 行：代码为 `if isinstance(cached, dict):`。开始条件分支，决定是否走缓存、校验、异常或降级路径。
- 第 52 行：代码为 `try:`。开始异常保护块。
- 第 53 行：代码为 `return InterviewSession.model_validate(cached)`。结束当前分支或函数并返回结果。
- 第 54 行：代码为 `except ValueError:`。捕获指定异常并进入项目定义的回滚或转换路径。
- 第 55 行：代码为 `await self._cache.delete(self._cache_key(session_id))`。等待异步 I/O 完成，不阻塞事件循环。
- 第 56 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 57 行：代码为 `entity = await db_session.scalar(`。这一行计算、保存或传递当前步骤所需的数据。
- 第 58 行：代码为 `select(InterviewSessionEntity).where(`。开始构造 SQLAlchemy SQL 表达式，尚未执行。
- 第 59 行：代码为 `InterviewSessionEntity.session_id == session_id`。这一行计算、保存或传递当前步骤所需的数据。
- 第 60 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 61 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 62 行：代码为 `session = self._from_entity(entity) if entity is not None else None`。这一行计算、保存或传递当前步骤所需的数据。
- 第 63 行：代码为 `if session is not None:`。开始条件分支，决定是否走缓存、校验、异常或降级路径。
- 第 64 行：代码为 `await self._cache_session(session)`。等待异步 I/O 完成，不阻塞事件循环。
- 第 65 行：代码为 `return session`。结束当前分支或函数并返回结果。

#### 3.3.4 `PostgresInterviewSessionRepository.save`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:67-68`

功能：以版本号乐观锁保存会话并刷新缓存。

逐行解释：

- 第 67 行：代码为 `async def save(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 68 行：代码为 `self, session: InterviewSession, *, expected_version: int`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.3.5 `PostgresInterviewSessionRepository._cache_key`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:105-108`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 105 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 106 行：代码为 `def _cache_key(session_id: str) -> str:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 107 行：代码为 `return f"python:interview-session:{session_id}"`。结束当前分支或函数并返回结果。

#### 3.3.6 `PostgresInterviewSessionRepository._cache_session`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:109-114`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 109 行：代码为 `async def _cache_session(self, session: InterviewSession) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 110 行：代码为 `if self._cache:`。开始条件分支，决定是否走缓存、校验、异常或降级路径。
- 第 111 行：代码为 `await self._cache.set_json(`。等待异步 I/O 完成，不阻塞事件循环。
- 第 112 行：代码为 `self._cache_key(session.session_id), session.model_dump(mode="json"), ttl_seconds=7200`。这一行计算、保存或传递当前步骤所需的数据。
- 第 113 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.3.7 `PostgresInterviewSessionRepository._to_entity`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:115-127`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 115 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 116 行：代码为 `def _to_entity(session: InterviewSession) -> InterviewSessionEntity:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 117 行：代码为 `return InterviewSessionEntity(`。结束当前分支或函数并返回结果。
- 第 118 行：代码为 `session_id=session.session_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 119 行：代码为 `user_id=session.user_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 120 行：代码为 `status=session.status,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 121 行：代码为 `current_stage=session.current_stage,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 122 行：代码为 `state_version=session.state_version,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 123 行：代码为 `session_data=session.model_dump(mode="json"),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 124 行：代码为 `created_at=session.created_at,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 125 行：代码为 `updated_at=session.updated_at,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 126 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.3.8 `PostgresInterviewSessionRepository._from_entity`

文件：`python-agent/app/infrastructure/persistence/interview_session_repository.py:128-130`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 128 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 129 行：代码为 `def _from_entity(entity: InterviewSessionEntity) -> InterviewSession:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 130 行：代码为 `return InterviewSession.model_validate(entity.session_data)`。结束当前分支或函数并返回结果。

### 3.4 `python-agent/app/infrastructure/persistence/long_term_memory_repository.py` 完整源码

~~~python
"""基于 PostgreSQL 的用户长期记忆持久化。"""

from datetime import datetime, timezone

from sqlalchemy import DateTime, Integer, JSON, String, select, update
from sqlalchemy.exc import IntegrityError
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker
from sqlalchemy.orm import Mapped, mapped_column

from app.memory.models import LongTermMemory
from app.memory.repository import LongTermMemoryRepository
from app.common.exceptions import ConsistencyError

from .interview_session_repository import Base


class LongTermMemoryEntity(Base):
    __tablename__ = "agent_long_term_memories"

    user_id: Mapped[str] = mapped_column(String(128), primary_key=True)
    state_version: Mapped[int] = mapped_column(Integer, nullable=False)
    memory_data: Mapped[dict] = mapped_column(JSON, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)


class PostgresLongTermMemoryRepository(LongTermMemoryRepository):
    def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -> None:
        self._session_factory = session_factory

    async def get(self, user_id: str) -> LongTermMemory | None:
        async with self._session_factory() as db_session:
            entity = await db_session.scalar(
                select(LongTermMemoryEntity).where(
                    LongTermMemoryEntity.user_id == user_id
                )
            )
        return LongTermMemory.model_validate(entity.memory_data) if entity else None

    async def create(self, memory: LongTermMemory) -> LongTermMemory:
        try:
            async with self._session_factory() as db_session:
                db_session.add(self._to_entity(memory))
                await db_session.commit()
        except IntegrityError as error:
            raise ConsistencyError("用户长期记忆已存在") from error
        return memory

    async def save(
        self, memory: LongTermMemory, *, expected_version: int
    ) -> LongTermMemory:
        saved = memory.model_copy(
            update={
                "state_version": expected_version + 1,
                "updated_at": datetime.now(timezone.utc),
            }
        )
        statement = (
            update(LongTermMemoryEntity)
            .where(
                LongTermMemoryEntity.user_id == saved.user_id,
                LongTermMemoryEntity.state_version == expected_version,
            )
            .values(
                state_version=saved.state_version,
                memory_data=saved.model_dump(mode="json"),
                updated_at=saved.updated_at,
            )
        )
        async with self._session_factory() as db_session:
            result = await db_session.execute(statement)
            if result.rowcount != 1:
                await db_session.rollback()
                raise ConsistencyError("用户长期记忆已被并发修改")
            await db_session.commit()
        return saved

    @staticmethod
    def _to_entity(memory: LongTermMemory) -> LongTermMemoryEntity:
        return LongTermMemoryEntity(
            user_id=memory.user_id,
            state_version=memory.state_version,
            memory_data=memory.model_dump(mode="json"),
            created_at=memory.created_at,
            updated_at=memory.updated_at,
        )
~~~

#### 3.4.1 `PostgresLongTermMemoryRepository.__init__`

文件：`python-agent/app/infrastructure/persistence/long_term_memory_repository.py:28-30`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 28 行：代码为 `def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 29 行：代码为 `self._session_factory = session_factory`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.4.2 `PostgresLongTermMemoryRepository.get`

文件：`python-agent/app/infrastructure/persistence/long_term_memory_repository.py:31-39`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 31 行：代码为 `async def get(self, user_id: str) -> LongTermMemory | None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 32 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 33 行：代码为 `entity = await db_session.scalar(`。这一行计算、保存或传递当前步骤所需的数据。
- 第 34 行：代码为 `select(LongTermMemoryEntity).where(`。开始构造 SQLAlchemy SQL 表达式，尚未执行。
- 第 35 行：代码为 `LongTermMemoryEntity.user_id == user_id`。这一行计算、保存或传递当前步骤所需的数据。
- 第 36 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 37 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 38 行：代码为 `return LongTermMemory.model_validate(entity.memory_data) if entity else None`。结束当前分支或函数并返回结果。

#### 3.4.3 `PostgresLongTermMemoryRepository.create`

文件：`python-agent/app/infrastructure/persistence/long_term_memory_repository.py:40-48`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 40 行：代码为 `async def create(self, memory: LongTermMemory) -> LongTermMemory:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 41 行：代码为 `try:`。开始异常保护块。
- 第 42 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 43 行：代码为 `db_session.add(self._to_entity(memory))`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 44 行：代码为 `await db_session.commit()`。等待异步 I/O 完成，不阻塞事件循环。
- 第 45 行：代码为 `except IntegrityError as error:`。捕获指定异常并进入项目定义的回滚或转换路径。
- 第 46 行：代码为 `raise ConsistencyError("用户长期记忆已存在") from error`。抛出异常，把失败明确传给上层。
- 第 47 行：代码为 `return memory`。结束当前分支或函数并返回结果。

#### 3.4.4 `PostgresLongTermMemoryRepository.save`

文件：`python-agent/app/infrastructure/persistence/long_term_memory_repository.py:49-50`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 49 行：代码为 `async def save(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 50 行：代码为 `self, memory: LongTermMemory, *, expected_version: int`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.4.5 `PostgresLongTermMemoryRepository._to_entity`

文件：`python-agent/app/infrastructure/persistence/long_term_memory_repository.py:78-86`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 78 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 79 行：代码为 `def _to_entity(memory: LongTermMemory) -> LongTermMemoryEntity:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 80 行：代码为 `return LongTermMemoryEntity(`。结束当前分支或函数并返回结果。
- 第 81 行：代码为 `user_id=memory.user_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 82 行：代码为 `state_version=memory.state_version,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 83 行：代码为 `memory_data=memory.model_dump(mode="json"),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 84 行：代码为 `created_at=memory.created_at,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 85 行：代码为 `updated_at=memory.updated_at,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 86 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

### 3.5 `python-agent/app/infrastructure/persistence/rag_vector_repository.py` 完整源码

~~~python
"""基于 PostgreSQL/pgvector 的 RAG 向量仓库。"""

from sqlalchemy import Integer, JSON, String, Text, delete, select
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker
from sqlalchemy.orm import Mapped, mapped_column
from pgvector.sqlalchemy import Vector

from app.rag.models import KnowledgeChunk, RagSearchResult
from app.rag.repository import VectorRepository

from .interview_session_repository import Base


class RagChunkEntity(Base):
    __tablename__ = "agent_rag_chunks"

    chunk_id: Mapped[str] = mapped_column(String(256), primary_key=True)
    knowledge_base_id: Mapped[str] = mapped_column(String(128), nullable=False, index=True)
    document_id: Mapped[str] = mapped_column(String(256), nullable=False, index=True)
    source_name: Mapped[str] = mapped_column(String(512), nullable=False)
    chunk_index: Mapped[int] = mapped_column(Integer, nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    chunk_metadata: Mapped[dict] = mapped_column(JSON, nullable=False)
    embedding: Mapped[list[float]] = mapped_column(Vector(), nullable=False)


class PostgresRagVectorRepository(VectorRepository):
    def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -> None:
        self._session_factory = session_factory

    async def delete_by_knowledge_base(self, knowledge_base_id: str) -> None:
        from sqlalchemy import delete

        async with self._session_factory() as db_session:
            await db_session.execute(
                delete(RagChunkEntity).where(
                    RagChunkEntity.knowledge_base_id == knowledge_base_id
                )
            )
            await db_session.commit()

    async def add(self, chunks: list[KnowledgeChunk]) -> None:
        async with self._session_factory() as db_session:
            db_session.add_all([self._to_entity(chunk) for chunk in chunks])
            await db_session.commit()

    async def replace_for_knowledge_base(
        self, knowledge_base_id: str, chunks: list[KnowledgeChunk]
    ) -> None:
        async with self._session_factory() as db_session:
            await db_session.execute(
                delete(RagChunkEntity).where(
                    RagChunkEntity.knowledge_base_id == knowledge_base_id
                )
            )
            db_session.add_all([self._to_entity(chunk) for chunk in chunks])
            await db_session.commit()

    async def search(
        self,
        query_embedding: list[float],
        *,
        top_k: int,
        min_score: float,
        knowledge_base_ids: tuple[str, ...],
        apply_metadata_filter: bool = True,
    ) -> list[RagSearchResult]:
        distance = RagChunkEntity.embedding.cosine_distance(query_embedding)
        score = (1 - distance).label("score")
        statement = select(RagChunkEntity, score)
        if apply_metadata_filter and knowledge_base_ids:
            statement = statement.where(
                RagChunkEntity.knowledge_base_id.in_(knowledge_base_ids)
            )
        statement = (
            statement.where(score >= min_score)
            .order_by(distance)
            .limit(top_k)
        )
        async with self._session_factory() as db_session:
            rows = (await db_session.execute(statement)).all()
        return [
            RagSearchResult(chunk=self._from_entity(entity), score=float(raw_score))
            for entity, raw_score in rows
        ]

    @staticmethod
    def _to_entity(chunk: KnowledgeChunk) -> RagChunkEntity:
        return RagChunkEntity(
            chunk_id=chunk.chunk_id,
            knowledge_base_id=chunk.knowledge_base_id,
            document_id=chunk.document_id,
            source_name=chunk.source_name,
            chunk_index=chunk.chunk_index,
            content=chunk.content,
            chunk_metadata=chunk.metadata,
            embedding=chunk.embedding,
        )

    @staticmethod
    def _from_entity(entity: RagChunkEntity) -> KnowledgeChunk:
        return KnowledgeChunk(
            chunk_id=entity.chunk_id,
            knowledge_base_id=entity.knowledge_base_id,
            document_id=entity.document_id,
            source_name=entity.source_name,
            chunk_index=entity.chunk_index,
            content=entity.content,
            metadata=entity.chunk_metadata,
            embedding=list(entity.embedding),
        )
~~~

#### 3.5.1 `PostgresRagVectorRepository.__init__`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:28-30`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 28 行：代码为 `def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 29 行：代码为 `self._session_factory = session_factory`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.5.2 `PostgresRagVectorRepository.delete_by_knowledge_base`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:31-41`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 31 行：代码为 `async def delete_by_knowledge_base(self, knowledge_base_id: str) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 32 行：代码为 `from sqlalchemy import delete`。这一行计算、保存或传递当前步骤所需的数据。
- 第 34 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 35 行：代码为 `await db_session.execute(`。等待异步 I/O 完成，不阻塞事件循环。
- 第 36 行：代码为 `delete(RagChunkEntity).where(`。开始构造 SQLAlchemy SQL 表达式，尚未执行。
- 第 37 行：代码为 `RagChunkEntity.knowledge_base_id == knowledge_base_id`。这一行计算、保存或传递当前步骤所需的数据。
- 第 38 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 39 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 40 行：代码为 `await db_session.commit()`。等待异步 I/O 完成，不阻塞事件循环。

#### 3.5.3 `PostgresRagVectorRepository.add`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:42-46`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 42 行：代码为 `async def add(self, chunks: list[KnowledgeChunk]) -> None:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 43 行：代码为 `async with self._session_factory() as db_session:`。进入异步数据库资源/事务上下文，退出时由上下文管理器清理。
- 第 44 行：代码为 `db_session.add_all([self._to_entity(chunk) for chunk in chunks])`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。
- 第 45 行：代码为 `await db_session.commit()`。等待异步 I/O 完成，不阻塞事件循环。

#### 3.5.4 `PostgresRagVectorRepository.replace_for_knowledge_base`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:47-48`

功能：原子替换一个知识库的分块。

逐行解释：

- 第 47 行：代码为 `async def replace_for_knowledge_base(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 48 行：代码为 `self, knowledge_base_id: str, chunks: list[KnowledgeChunk]`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.5.5 `PostgresRagVectorRepository.search`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:59-66`

功能：执行向量检索并映射结果。

逐行解释：

- 第 59 行：代码为 `async def search(`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 60 行：代码为 `self,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 61 行：代码为 `query_embedding: list[float],`。这一行计算、保存或传递当前步骤所需的数据。
- 第 62 行：代码为 `*,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 63 行：代码为 `top_k: int,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 64 行：代码为 `min_score: float,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 65 行：代码为 `knowledge_base_ids: tuple[str, ...],`。这一行计算、保存或传递当前步骤所需的数据。
- 第 66 行：代码为 `apply_metadata_filter: bool = True,`。这一行计算、保存或传递当前步骤所需的数据。

#### 3.5.6 `PostgresRagVectorRepository._to_entity`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:87-99`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 87 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 88 行：代码为 `def _to_entity(chunk: KnowledgeChunk) -> RagChunkEntity:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 89 行：代码为 `return RagChunkEntity(`。结束当前分支或函数并返回结果。
- 第 90 行：代码为 `chunk_id=chunk.chunk_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 91 行：代码为 `knowledge_base_id=chunk.knowledge_base_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 92 行：代码为 `document_id=chunk.document_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 93 行：代码为 `source_name=chunk.source_name,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 94 行：代码为 `chunk_index=chunk.chunk_index,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 95 行：代码为 `content=chunk.content,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 96 行：代码为 `chunk_metadata=chunk.metadata,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 97 行：代码为 `embedding=chunk.embedding,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 98 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

#### 3.5.7 `PostgresRagVectorRepository._from_entity`

文件：`python-agent/app/infrastructure/persistence/rag_vector_repository.py:100-111`

功能：执行本模块的装配、持久化、缓存或领域映射步骤。

逐行解释：

- 第 100 行：代码为 `@staticmethod`。这一行声明装饰器，使紧随其后的函数具有相应绑定或调用语义。
- 第 101 行：代码为 `def _from_entity(entity: RagChunkEntity) -> KnowledgeChunk:`。这一行定义函数的名称、参数、异步性质和返回类型。
- 第 102 行：代码为 `return KnowledgeChunk(`。结束当前分支或函数并返回结果。
- 第 103 行：代码为 `chunk_id=entity.chunk_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 104 行：代码为 `knowledge_base_id=entity.knowledge_base_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 105 行：代码为 `document_id=entity.document_id,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 106 行：代码为 `source_name=entity.source_name,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 107 行：代码为 `chunk_index=entity.chunk_index,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 108 行：代码为 `content=entity.content,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 109 行：代码为 `metadata=entity.chunk_metadata,`。这一行计算、保存或传递当前步骤所需的数据。
- 第 110 行：代码为 `embedding=list(entity.embedding),`。这一行计算、保存或传递当前步骤所需的数据。
- 第 111 行：代码为 `)`。续接或闭合前面的调用、集合或表达式，保持参数和数据完整。

## 4. 主流构建分析

当前实现使用 SQLAlchemy 异步 ORM、Repository 隔离、PostgreSQL 事务与版本字段乐观锁，并以 Redis 进行会话缓存。优点是业务服务不直接拼 SQL，数据库仍是唯一真相源，缓存失效不影响最终状态；缺点是 bootstrap 逐渐集中依赖图，领域对象与 ORM 实体的映射需要同步维护。

主流工程可用 FastAPI lifespan 与 Depends 管理引擎、Redis 客户端和服务实例，或引入显式依赖注入容器。优点是资源关闭、测试替身和依赖生命周期更清楚；缺点是增加框架抽象和配置复杂度。本项目适合渐进迁移：保留 build 函数和现有请求契约，在 lifespan 预建并在退出时关闭连接，再由 Depends 从应用状态取服务；保留现有乐观锁与“数据库保存成功后回填/失效缓存”的 cache-aside 规则，不能让 Redis 代替 PostgreSQL 的最终一致性。

