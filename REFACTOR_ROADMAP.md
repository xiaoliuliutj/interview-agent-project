# Interview Agent 后端重构路线

本文档定义后端从零构建的唯一执行顺序。前端直接复用，原始 InterviewGuide 仅作为业务和工程设计参考。

## 一、重构目标

构建一条可运行、可解释、可部署的后端链路：

    前端 → Java 业务后端 → Python Agent 服务 → 模型、RAG 与工具

最终系统需要完成面试资料管理、面试会话、Agent 出题与追问、回答评价、知识检索、记忆管理、异步任务和虚拟机部署。

## 二、执行原则

### 2.1 先确定边界，再实现功能

先定义前端、Java 后端和 Python Agent 的职责，统一数据契约和交互格式，避免功能重叠和重复设计。

### 2.2 先完成主业务，再集中补强工程化

业务主流程优先，但从第一条链路开始就必须具备最低限度的状态、日志、错误处理、超时和幂等约束。完整的一致性、并发、失败恢复和部署能力在主流程稳定后集中完善。

### 2.3 每个阶段都必须可运行

每个阶段都要有明确的输入、输出和验收结果，不能只完成目录、接口或代码框架。

## 三、四阶段重构路线

### 阶段一：全局框架与交互契约

目标：确定系统边界，形成后续实现依据。

需要完成：

- 明确前端、Java 业务后端和 Python Agent 的职责。
- 确定资料、面试会话、任务、记忆和 Agent 运行记录的关系。
- 确定 Java 调用 Python 的请求、响应、错误和流式事件。
- 确定统一的请求标识、会话标识和任务标识。
- 确定前端依赖的业务接口和展示状态。
- 选定一条最小演示链路。

最小演示链路：

    创建面试会话 → 输入面试目标 → Agent 生成一次针对性问题 → 前端展示

阶段产出：

- 总体架构说明
- 业务边界说明
- 前后端交互契约
- 数据对象关系
- 第一版验收样例

### 阶段二：Python Agent 服务

目标：独立完成可运行、可测试的 Agent 服务，不等待 Java 后端全部完成。

开发方式：

- 使用固定测试上下文模拟简历、岗位描述、历史记录和知识检索结果。
- 使用适配器隔离模型调用和 Java 业务数据来源。
- 先完成文本面试场景，不扩展复杂语音能力。

需要完成：

- Agent 服务启动和健康检查。
- Agent 状态、运行入口和终止条件。
- Agent Loop、Tools、Skill 和一个可演示的 MCP 场景。
- 基础 RAG 检索和来源信息。
- 短期记忆与上下文管理。
- 结构化结果和流式事件。
- 基础超时、重试、错误分类和执行日志。

阶段验收：

- Python Agent 可以独立启动。
- 不依赖 Java 也能完成一次完整 Agent 运行。
- Agent 能根据任务选择工具并处理工具结果。
- 模型、工具或检索失败时有可解释结果。
- 固定测试数据可以重复验证结果。

### 阶段三：Java 业务后端与前端接通

目标：从零构建 Java 业务后端，将 Python Agent 接入真实业务数据，并保持前端可用。

实现顺序：

1. 建立 Java 项目骨架、配置、统一响应和异常边界。
2. 建立资料、面试会话、问题、回答和评价的数据模型。
3. 实现前端需要的资料、会话、问题和回答接口。
4. 实现 Java 到 Python Agent 的适配层。
5. 将固定测试上下文替换为 Java 提供的真实业务数据。
6. 接通前端，完成完整文本面试主流程。

最低工程化要求：

- 数据库事务和状态流转。
- 参数校验和统一错误。
- 服务调用超时和失败映射。
- 请求标识和结构化日志。
- 关键操作的幂等约束。
- Java/Python 接口契约兼容。
- Java 持久化业务任务和业务结果；Python 持久化 Agent 运行记录、记忆、RAG 数据和 Agent 领域任务状态。

阶段验收：

- 前端无需重构即可完成核心页面交互。
- Java 能组装标准上下文并调用 Python。
- Python 结果能被 Java 持久化并返回前端。
- 浏览器发起的请求可以完整经过前端、Java 和 Python。

### 阶段四：全面完善工程化与部署

目标：围绕已经跑通的主流程，补齐稳定性、可恢复性和部署能力。

完善顺序：

1. 资料解析、向量化、评价和报告生成的异步任务。
2. 任务状态查询、重试、超时、取消和恢复。
3. 数据库与任务队列之间的一致性。
4. 重复提交、重复消费和重复 Agent 运行的幂等控制。
5. 会话级并发控制和整体资源限制。
6. 模型、RAG、MCP、数据库失败的分类处理。
7. Agent 运行轨迹、耗时、错误和质量记录。
8. 服务健康检查、持久化、备份和重启恢复。
9. 虚拟机部署、全链路测试和故障演示。

阶段验收：

- 系统可以在虚拟机中启动和访问。
- 主业务链路可重复演示。
- 任务失败、服务重启和重复请求有明确处理结果。
- 关键运行过程能够通过日志和运行记录复盘。

## 四、阶段依赖关系

    全局契约
       ↓
    Python Agent 独立能力
       ↓
    Java 业务数据与前端接入
       ↓
    异步、一致性、并发、失败恢复和部署

后一个阶段不得绕过前一个阶段的验收结果。

## 五、面试材料同步产出

每个阶段完成后同步整理：

- 本阶段解决的问题
- 主要设计和技术选型
- 前后端交互链路
- 异常和边界处理
- 可量化或可验证的结果
- 面试时的讲解方式
