<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interviewguide.resume.mapper.ResumeRepository">
  <select id="findById" resultType="com.interviewguide.resume.domain.ResumeEntity">SELECT * FROM resumes WHERE id=#{id}</select>
  <select id="findAll" resultType="com.interviewguide.resume.domain.ResumeEntity">SELECT * FROM resumes</select>
  <select id="findFirstByCandidateIdAndFileHash" resultType="com.interviewguide.resume.domain.ResumeEntity">SELECT * FROM resumes WHERE candidate_id=#{candidateId} AND file_hash=#{fileHash} ORDER BY version DESC LIMIT 1</select>
  <select id="findByCandidateId" resultType="com.interviewguide.resume.domain.ResumeEntity">SELECT * FROM resumes WHERE candidate_id=#{candidateId} ORDER BY version</select>
  <select id="findFirstByCandidateIdOrderByVersionDesc" resultType="com.interviewguide.resume.domain.ResumeEntity">SELECT * FROM resumes WHERE candidate_id=#{candidateId} ORDER BY version DESC LIMIT 1</select>
  <insert id="upsert">INSERT INTO resumes (id,candidate_id,version,content,file_hash,original_filename,file_size,content_type,storage_key,created_at) VALUES (#{id},#{candidateId},#{version},#{content},#{fileHash},#{originalFilename},#{fileSize},#{contentType},#{storageKey},#{createdAt}) ON CONFLICT (id) DO UPDATE SET candidate_id=EXCLUDED.candidate_id,version=EXCLUDED.version,content=EXCLUDED.content,file_hash=EXCLUDED.file_hash,original_filename=EXCLUDED.original_filename,file_size=EXCLUDED.file_size,content_type=EXCLUDED.content_type,storage_key=EXCLUDED.storage_key</insert>
  <delete id="delete">DELETE FROM resumes WHERE id=#{id}</delete>
</mapper>
